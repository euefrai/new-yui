## Copie este arquivo para ".env" e preencha os valores.
##
## Windows (PowerShell):
##   Copy-Item .env.example .env
##
## Linux/macOS:
##   cp .env.example .env

# Obrigatório para respostas via OpenAI (opcional se você não for usar conversa por IA)
OPENAI_API_KEY=coloque_sua_chave_aqui

# Opcional: modelo padrão (se não informado, o código usa um default)
# OPENAI_MODEL=gpt-4o-mini

# Supabase (login e chats por usuário) — use duas keys para segurança
# SUPABASE_URL=https://seu-projeto.supabase.co
# SUPABASE_ANON_KEY=chave_anon (pública) — só no frontend / navegador
# SUPABASE_SERVICE_KEY=chave_service_role — só no backend; NUNCA expor no frontend
# Opcional (fallback): SUPABASE_PUBLISHABLE_KEY ou NEXT_PUBLIC_SUPABASE_* para anon
# Memória: USE_SUPABASE_MEMORY=true usa Supabase; false ou omitido usa JSON local (data/chats.json)
# USE_SUPABASE_MEMORY=true

# Energy Manager (freio cognitivo) — ajuste se estiver vendo "Energia esgotada" com frequência
# ENERGY_MAX=180
# ENERGY_LOW_THRESHOLD=36
# ENERGY_CRITICAL_THRESHOLD=18
# ENERGY_MIN_BOOT=45
# ENERGY_COST_RESPONDER_IA=8
# ENERGY_COST_TOOL=10
# ENERGY_COST_PLANNER=3
# ENERGY_COST_REFLECT=2
# ENERGY_COST_RECOVERY=10

# PostgreSQL direto (opcional; senha com @ use %40 na URL)
# DATABASE_URL=postgresql://postgres:SUA_SENHA@db.SEU_PROJETO.supabase.co:5432/postgres
